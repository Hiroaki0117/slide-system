---
spec_name: slide-system-qa
version: 0.1.1
status: draft
updated: 2026-08-04
language: ja
---

# Quality Assurance Specification

## 1. 目的

この仕様は、スライド制作の着手前確認、完成前検査、修正ループ、納品判定を定義します。

ファイルを生成できたことだけを完成とせず、依頼適合、内容、デザイン、ビジュアル、出力形式を検査し、必須条件が合格した成果物だけを完成として扱うことを目的とします。

各分野の具体的な値と制作ルールは、`10_CONTENT.md`、`20_DESIGN.md`、`30_LAYOUTS.md`、`40_VISUALS.md`、`50_OUTPUTS.md`を参照します。この仕様では同じ値を重複定義せず、選択された仕様への準拠を判定します。

## 2. 品質ゲート

QAは次の順序で実行します。

| ゲート | 名称 | 実行時期 | 主な目的 |
|---|---|---|---|
| 0 | 着手前の実行可能性 | 本制作前 | 不足、権利、生成・検査能力を早期に確認する |
| 1 | 依頼適合 | 制作後 | 確認済みの制作条件と成果物を照合する |
| 2 | 内容・構成 | 制作後 | 事実性、ストーリー、ページの役割を確認する |
| 3 | デザイン・レイアウト・ビジュアル | 制作後 | 選択テーマと視覚仕様への準拠を確認する |
| 4 | 出力形式 | 生成・変換後 | HTML、PPTX、PDFを形式ごとに確認する |
| 5 | 納品・完了判定 | 納品直前 | 最終成果物とQA結果を確定する |

## 3. 判定状態

| 状態 | 意味 |
|---|---|
| `PASS` | 条件を満たしている |
| `FAIL` | 必須条件を満たしていない |
| `WARN` | 納品可能だが、注意または改善余地がある |
| `N/A` | 今回の依頼では対象外 |
| `NOT_VERIFIED` | 環境または情報不足により確認できていない |

- `MUST`: 必須項目は、`PASS`または理由を記録した`N/A`でなければ合格としない。
- `MUST`: 必須項目の`NOT_VERIFIED`を推測で`PASS`へ変更しない。
- `MUST`: `FAIL`が残る成果物を完成と報告しない。
- `SHOULD`: 安全に改善できる`WARN`は、納品前に修正する。
- `SHOULD`: 意図的な表現または合理的なトレードオフによる`WARN`は、理由を記録して納品時に伝える。

## 4. ゲート0：着手前の実行可能性

本制作へ入る前に、依頼内容、入力ファイル、指定素材、出力プロファイルを確認します。

### 4.1 入力と元資料

- `MUST`: 必須の元資料をすべて開き、内容を読み取れるか確認する。
- `MUST`: ファイルが破損、暗号化、空データになっていないか確認する。
- `MUST`: 必要なページ、画像、数値、指示が揃っているか確認する。
- `MUST`: 指定された写真、ロゴ、テンプレート、ブランド資料へアクセスできるか確認する。
- `MUST`: 依頼条件同士に、成果物へ影響する矛盾がないか確認する。

### 4.2 素材と利用条件

- `MUST`: 提供素材に記録された出典と利用条件を確認できるか確認する。
- `MUST`: 明らかな権利上の懸念、透かし、第三者の機密情報がないか確認する。
- `MUST`: 利用条件を確認できない依頼者指定素材を、無断で置き換えない。

この確認は、法的な権利を保証するものではありません。確認できる情報と用途の範囲で問題を早期発見し、判断が必要な場合は依頼者へ伝えます。

### 4.3 生成・変換・検査能力

- `MUST`: 指定された主成果物を生成できる手段があるか確認する。
- `MUST`: 指定された派生成果物へ変換できる手段があるか確認する。
- `MUST`: 生成するすべての形式を開き、全ページを検査できるか確認する。
- `MUST`: 必要なフォント、素材、実行機能を利用できるか確認する。
- `SHOULD`: 初めて使用する環境または出力経路では、本制作前に1ページの生成・変換テストを行う。

### 4.4 提案付きの一時停止

安全に自動解決できず、成果物の内容、形式、指定素材へ影響する問題がある場合は、本制作を確定する前にユーザーの判断を求めます。

伝える内容は次のとおりです。

1. 発見した問題
2. 成果物への影響
3. 推奨する対応
4. 代替案とその違い
5. ユーザーに必要な操作または判断

- `MUST`: 推奨案を先頭にし、原則2〜3個の選択肢を提示する。
- `MUST`: ユーザーの同意なく、出力形式、指定素材、対象範囲を変更しない。
- `MUST`: 依頼者指定素材を類似素材やAI生成素材へ無断で置き換えない。
- `SHOULD`: 選択が必要な部分以外は、手戻りを生まない範囲で準備できる。
- `MUST`: ユーザーの回答後にゲート0を再実行し、問題が解消したことを確認する。

問題別の提案例は次のとおりです。

| 問題 | 主な提案 |
|---|---|
| 元資料不足 | 再添付、対象範囲の縮小、不足部分を明示した仮制作 |
| ファイルを開けない | 再保存、別形式への書き出し、解除済みファイルの提供 |
| 利用条件を確認できない | 公式素材、条件が明確な素材、自作図解、AI生成素材への変更 |
| 指定形式を生成できない | 別環境での変換、単一形式での納品、プロファイル変更 |
| フォントを利用できない | 定義済み代替フォントと見た目への影響を提示 |
| 条件が矛盾する | 目的に最も合う解釈を推奨し、他案との差を提示 |
| 画像の解像度不足 | 高解像度版の提供、表示縮小、別素材への変更 |
| 情報不足 | 不足情報の提供、該当部分の削除、未確定表示での仮制作 |

### 4.5 制作承認

- `MUST`: 不足情報への質問と制作条件への承認を別のターンで行っている。
- `MUST`: 質問への回答または訂正情報を、制作承認として扱っていない。
- `MUST`: 最終的な制作条件に対するユーザーの明示的な承認、または確認不要という明示的な指示を記録している。
- `MUST`: 承認前の外部確認を、元資料、大会名、日付など制作条件を正確にするための最小限の読み取りに限定している。
- `MUST`: 承認前に広い内容調査、構成作成、内容モデル作成、HTML/PPTX生成、レンダリング、PDF出力を開始していない。
- `MUST`: 生成スクリプトに承認ゲートがある場合、承認記録を渡して合格している。

## 5. 検査範囲と確認方法

### 5.1 全ページ検査

最終成果物は、代表ページだけでなく全ページを確認します。

- `MUST`: タイトル、本文、画像、図形、ページ番号、出典の欠落を全ページで確認する。
- `MUST`: 重なり、はみ出し、意図しない改行、読めない文字を全ページで確認する。
- `MUST`: 画像の解像度、縦横比、切り抜きを全ページで確認する。
- `MUST`: グラフと表の数値、単位、ラベルを該当する全ページで確認する。
- `MUST`: 仮画像、仮テキスト、制作メモ、未解決プレースホルダーが残っていないか確認する。

代表ページだけの確認は、テーマまたはレイアウトの試作段階に限定します。最終納品のQAではサンプリングを行いません。

### 5.2 資料全体の確認

- `MUST`: 全ページの一覧表示で、ストーリー、章の切り替わり、見た目の一貫性を確認する。
- `MUST`: 一覧表示だけで合格とせず、各ページを実際の表示サイズで個別に確認する。
- `MUST`: 自動検査が利用できる場合は実行するが、自動検査の結果だけで合格としない。
- `MUST`: 納品するすべての出力形式について全ページを確認する。

## 6. ゲート1：依頼適合

判定の基準は、次の順序とします。

1. ユーザーが最後に明示した指示
2. 作成前に確認した制作条件
3. 添付された元資料、テンプレート、ブランドガイド
4. 各仕様書の既定値

### 6.1 `MUST`

- 指定されたテーマまたは元資料を使用している。
- 対象者に合った説明水準になっている。
- 利用目的を満たす構成になっている。
- 中心メッセージが資料全体で維持されている。
- 確認済みのコンテンツモードと資料タイプが適用されている。
- 指定または確認済みの枚数、発表時間、言語、画面比率に対応している。
- 表紙にタイトルと日付があり、指定された任意項目が反映されている。
- 指定されたテンプレート、ブランド、ロゴ、写真、データを使用している。
- 使用しないよう指定された内容または素材が含まれていない。
- 指定された出力形式が実際に生成されている。
- ユーザーの同意なく別形式、別テーマ、別素材へ変更していない。

### 6.2 `SHOULD`

- 対象者が理解しやすい語彙と説明量になっている。
- 利用場面に合った情報密度になっている。
- ユーザーが重視した内容へ十分な比重を置いている。
- 指定のなかった細部に、安全な既定値を適用している。
- 依頼範囲を超えた不要な情報を追加していない。

## 7. ゲート2：内容・構成

### 7.1 事実性の`MUST`

- 元資料にない事実、数値、引用、人物、事例を創作していない。
- 元資料の意味を変える要約をしていない。
- 事実とAIによる解釈または提案を区別している。
- 数値の単位、期間、対象、合計値が一致している。
- グラフ、表、本文の数値に矛盾がない。
- 外部情報と外部素材の出典を追跡できる。
- 元資料同士の矛盾を隠していない。
- 確認できない主張を根拠なく断定していない。
- 予測式、計算値、ベンチマーク、過去の単一実績を、結果の保証または達成可能性の断定に使っていない。
- 計算値や予測には入力、前提、丸め方、主要な限界が対応するページに示されている。
- 日付間の期間を正確に計算し、資料内で一貫した単位と丸め方を使用している。
- 高リスク分野の提案は、根拠が直接支える範囲を超えて個別化していない。

### 7.2 資料全体の`MUST`

- 対象者と目的に合うストーリーになっている。
- 冒頭で、資料を見る理由または扱う問いが分かる。
- 各章が前後につながり、項目の羅列になっていない。
- 中心メッセージと関係のないページが含まれていない。
- 同じ説明または結論を不必要に繰り返していない。
- 最後のページが、結論、行動、理解確認など目的に合う形で終わっている。
- 制作メモ、プロンプト、内部的な判断理由が読者向け本文に残っていない。

### 7.3 ページ単位の`MUST`

- 各ページに主要ジョブが1つ設定されている。
- 各ページの主要メッセージが1つに絞られている。
- タイトルと本文が同じ内容を伝えている。
- タイトルが可能な範囲で要点を伝えている。
- データと事例が、そのページの主張を支えている。
- 前後のページとの論理的なつながりがある。
- 収容量を超えた場合、文字縮小ではなく分割または要約されている。

### 7.4 モード・資料タイプ別の確認

- `standalone`: 発表者の説明がなくても論旨を理解できる。
- `presented`: 投影時に読み切れない文章を載せすぎていない。
- `explain`: 扱う問いと説明結果が対応している。
- `proposal`: 課題、提案、根拠、次の行動がつながっている。
- `report`: 結果だけでなく、その意味または示唆が示されている。
- `how_to`: 手順、順序、注意点、完了状態が分かる。
- 段階的なロードマップ: 開始条件、進行条件、維持または回復、後退条件、中止または相談条件が分かる。
- `training`: 学習目標、説明、演習、理解確認、まとめが適切に含まれている。

## 8. ゲート3：デザイン・レイアウト・ビジュアル

このゲートはテーマ非依存です。選択されたテーマの仕様を検査し、特定テーマの色名や図形をQA側で固定しません。

### 8.1 デザインの`MUST`

- 確認済みのテーマ、テンプレート、ブランド指定が適用されている。
- 選択されていないテーマの表現が意図せず混在していない。
- 選択テーマのフォント、色、背景、強調方法が統一されている。
- 最小文字サイズとコントラストの共通要件を満たしている。
- 色だけで分類、状態、重要度を伝えていない。
- 装飾がタイトル、本文、図表より目立っていない。
- ページ番号が全ページの共通位置に表示されている。
- 必要な出典が共通位置に表示されている。

### 8.2 レイアウトの`MUST`

- ページの主要ジョブに合ったレイアウトを選んでいる。
- レイアウトの収容量を超えていない。
- タイトル、本文、画像、図形に意図しない重なりがない。
- 文字または画像がスライド外へはみ出していない。
- 1行を想定したタイトルが意図せず折り返していない。
- 重要要素が安全余白の外に出ていない。
- 同じ役割の要素が共通の整列線に揃っている。
- 文字を小さくして無理に内容を収めていない。
- 図解の線がノードまたは文字を横切っていない。

### 8.3 ビジュアルの`MUST`

- 各ページのビジュアル役割が内容と一致している。
- 依頼者指定素材が正しく使用されている。
- 画像の縦横比、画質、切り抜きに問題がない。
- 写真、イラスト、アイコンのスタイルが、選択テーマ内で統一されている。
- グラフと表の数値、単位、ラベル、出典が正しい。
- スクリーンショット内に個人情報または機密情報が残っていない。
- 外部素材とAI生成素材の出典、提供元、利用条件を追跡できる。
- 仮画像、透かし、不鮮明な素材が残っていない。

### 8.4 資料全体の`SHOULD`

- ページ間で適度に見た目の変化がある。
- 同じレイアウトの連続使用に内容上の理由がある。
- 文字中心とビジュアル中心のページのバランスがよい。
- 余白が不足または過剰になっていない。
- 選択テーマの主要色と補助色の役割が一貫している。
- 装飾が選択テーマのトンマナと使用量に適合している。
- 1ページの中で最も強い強調が明確である。

## 9. ゲート4：出力形式

選択した出力プロファイルに応じて、`50_OUTPUTS.md`の対象形式だけを検査します。

### 9.1 共通の`MUST`

- 確認済みの出力プロファイルが使用されている。
- 主成果物が実際に生成され、正常に開ける。
- 指定された派生成果物が実際に生成されている。
- 主成果物と派生成果物のページ数、順序、内容が一致する。
- すべての納品形式について全ページを確認している。
- ファイルが破損していない。
- ファイル名と拡張子が納品内容と一致する。
- 未検査のファイルを完成済みとして扱っていない。

### 9.2 HTMLの`MUST`

- ブラウザで正常に開ける。
- 指定された画面比率のスライド単位で表示される。
- 最初から最後まで移動できる。
- ページ番号と現在位置が正しく更新される。
- 文字、画像、図表が表示領域内に収まる。
- 必要な内容が、想定した利用環境で表示される。
- 印刷時に1スライドが1ページになる。
- PDF出力時に操作UIが表示されない。

モバイル表示は、ユーザーが必要とする場合またはスマホ利用を前提とする場合に追加検査します。

### 9.3 PPTXの`MUST`

- PowerPoint互換環境で正常に開ける。
- 文字が可能な範囲で編集可能な状態になっている。
- 未解決のプレースホルダーが残っていない。
- フォント置換または改行崩れが発生していない。
- 画像、図形、グラフ、表が欠落していない。
- 発表者ノートが必要な場合、対応するページへ格納されている。
- 全ページを画像として表示し、個別に確認している。

編集可能性を求める形式で、資料全体を1枚の画像として貼り付けただけの場合は`FAIL`とします。

### 9.4 PDFの`MUST`

- 正常に開ける。
- 1スライドが1ページになっている。
- 主成果物とページ数、順序が一致している。
- 背景、文字、画像、図形、出典、ページ番号が欠落していない。
- スライド外の余白または操作UIが含まれていない。
- フォント、改行、色、透明度が不自然に変化していない。
- ページ端で内容が切れていない。

PDFだけを直接修正して主成果物と不一致にしてはなりません。主成果物を修正し、PDFを再生成します。

## 10. ゲート5：納品・完了判定

### 10.1 納品前の`MUST`

- 指定された成果物がすべて実際に存在する。
- 各ファイルが最終修正版である。
- 主成果物と派生成果物の内容が一致している。
- ファイル名、拡張子、ページ数が正しい。
- 仮ファイル、途中版、検証用画像を納品物へ混ぜていない。
- 元ファイルを残す必要がある場合、無断で上書きしていない。
- すべての品質ゲートを最終修正版に対して再実行している。
- 必須項目に`FAIL`または`NOT_VERIFIED`が残っていない。
- 生成していない形式を、生成済みと報告していない。

### 10.2 完成条件

次をすべて満たした場合だけ、完成と報告します。

1. 依頼された成果物が生成されている。
2. すべての`MUST`項目が`PASS`または理由を記録した`N/A`である。
3. 修正後の全体QAが完了している。
4. 必須の形式別検査が完了している。
5. 残っている`WARN`と、任意検査の`NOT_VERIFIED`が明示されている。

必須項目に問題が残る場合は、成果物が存在しても完成扱いにしません。

## 11. 修正ループ

QAは一度確認して終了せず、次の順序で繰り返します。

```text
成果物を生成
↓
QAを実行
↓
FAIL・WARN・NOT_VERIFIEDを分類
↓
修正可能な問題を修正
↓
再生成・再変換
↓
該当項目と関連ページを再検査
↓
最後に全体QAを再実行
```

- `MUST`: 必須項目の`FAIL`を修正し、再検査する。
- `MUST`: 修正後は問題のあったページだけでなく、関連ページと派生成果物も確認する。
- `MUST`: 最後に全ページ、全出力形式のQAを再実行する。
- `MUST`: すべての必須項目が`PASS`または`N/A`になるまで繰り返す。
- `MUST`: 修正回数に機械的な上限を設けない。
- `MUST`: 自力で解決できない問題が判明した場合は完成扱いにせず、原因と必要な対応を伝える。

## 12. QA記録

制作中は、少なくとも次を追跡できる形で記録します。記録形式は環境に合わせてよく、ユーザーが求めていない限り別の納品ファイルを増やしません。

| 項目 | 内容 |
|---|---|
| ゲート | 0〜5 |
| 対象 | 資料全体、ページ番号、出力ファイル |
| 重要度 | `MUST`、`SHOULD` |
| 判定 | `PASS`、`FAIL`、`WARN`、`N/A`、`NOT_VERIFIED` |
| 根拠 | 実行した確認または発見内容 |
| 対応 | 修正、再検査、例外理由、未解決事項 |

## 13. 納品時の報告

通常はQA記録をすべて表示せず、次を簡潔に伝えます。

- 使用した出力プロファイル
- 実際に生成したファイル
- 実施した主な検査
- QAの最終結果
- 残っている`WARN`
- 実施できなかった任意検査
- 既知の制約

詳細なQA記録は、ユーザーから求められた場合に提示します。

## 14. 完成できない場合

着手前には発見できなかった問題により必須項目を合格にできない場合は、次を伝えます。

1. 合格できなかった項目
2. 原因
3. 影響する成果物
4. 推奨する対応
5. 代替案とその違い
6. 現時点で生成できたファイルの状態

代替案は提案できますが、ユーザーの同意なく完成条件、対象範囲、指定素材、出力形式を変更しません。

## 15. 試作後に見直す項目

代表ケースの試作と実際の形式変換を行い、次を見直します。

- ゲート0の1ページ出力テストを必須にする条件
- QA記録の粒度と表示方法
- `WARN`として許容する範囲
- HTML、PPTX、PDFで利用できる自動検査
- 全ページ検査と修正ループに必要な時間
- 無料プランのClaudeで実行できる検査範囲

見直しによって必須検査を減らす場合は、品質への影響と代替確認方法を明示します。
