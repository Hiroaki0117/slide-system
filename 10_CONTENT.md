---
spec_name: slide-system-content
version: 0.1.0
status: draft
updated: 2026-08-04
language: ja
default_content_mode: standalone
default_deck_type: explain
default_audience_profile: general
---

# Content Specification

## 1. 目的

この仕様は、スライドの構成、文章、情報量、出典、表紙、終わり方、発表者ノート、付録の扱いを定義します。

デザインや出力形式にかかわらず、対象者が資料の目的に沿って理解、判断、実行または学習できる内容を作ることを目的とします。

## 2. コンテンツモード

コンテンツモードは、資料だけで理解させるか、発表者の説明と組み合わせるかを表します。出力プロファイルとは別に決定します。たとえば、`standalone`かつ`pptx_pdf`の組み合わせも使用できます。

| モード | 用途 | 内容の作り方 |
|---|---|---|
| `standalone` | 配布、送付、共有、報告書など、資料だけで読まれる場面 | スライド内に理解に必要な文脈、根拠、結論を残す |
| `presented` | 登壇、発表、ピッチ、セミナーなど、口頭説明を伴う場面 | 話す内容を重複させず、要点と視覚情報を中心にする |

既定モードは`standalone`です。

依頼に「登壇」「発表」「ピッチ」「セミナー」「口頭説明」などが含まれる場合は`presented`を選びます。「配布」「送付」「共有」「報告書」「相手に読んでもらう」などが含まれる場合は`standalone`を選びます。判断できない場合は質問を増やさず、`standalone`を使用し、制作前の確認に含めます。

## 3. 資料タイプ

利用目的から、次の資料タイプを1つ選びます。複数の要素がある場合も、主目的に対応するタイプを主タイプとします。

| タイプ | 主な目的 | 基本構成 |
|---|---|---|
| `explain` | 概念、状況、仕組みを理解してもらう | 背景 → 要点 → 仕組み → 例 → まとめ |
| `proposal` | 提案を検討し、判断または行動してもらう | 課題 → 根拠 → 選択肢 → 推奨案 → 行動 |
| `report` | 結果や変化を共有し、意味と対応を理解してもらう | 結論 → 事実 → 分析 → 意味 → 次の対応 |
| `how_to` | 手順を理解し、実行できるようにする | ゴール → 前提 → 手順 → 注意点 → 完了確認 |
| `training` | 学習し、練習と確認を通じてできるようにする | 学習目標 → 前提知識 → 概念 → 例・実演 → 演習 → 理解確認 → 振り返り・次の練習 |

`training`は、`explain`と`how_to`の要素を含みますが、学習目標、演習、理解確認が必要なため独立したタイプとして扱います。

資料タイプは利用目的から推定します。判断材料がなく安全に推定できない場合の既定値は`explain`です。

## 4. 対象者プロファイル

対象者の記述から、次のプロファイルを1つ選びます。判断できない場合の既定値は`general`です。

| プロファイル | 内容の調整 |
|---|---|
| `beginner` | 用語を定義し、一度に扱う概念を絞り、例と「なぜ必要か」を示す |
| `general` | 専門用語は説明し、一般知識は過度に説明しない |
| `practitioner` | 手順、判断基準、例外、実務での使い方を重視する |
| `expert` | 正確な用語、前提、方法、制約、データを示し、簡略化で精度を落とさない |
| `decision_maker` | 結論を早く示し、影響、費用、リスク、選択肢、必要な判断を重視する。技術的な詳細は付録へ回せる |

対象者が明確な場合は、プロファイル名を追加で質問しません。複数の対象者がいる場合は、説明水準を混在させず、主対象者を1つ定めます。

## 5. スライド単位の設計

### 5.1 1枚1ジョブ

各スライドには、次の主要ジョブを1つ設定します。

- `claim`: 主張や結論を伝える
- `explain`: 概念や仕組みを説明する
- `evidence`: 根拠や事実を示す
- `compare`: 選択肢や差を比較する
- `instruction`: 手順や行動を示す
- `question`: 問いを提示する
- `exercise`: 演習を実行してもらう
- `summary`: 要点をまとめる
- `transition`: 章や論点を切り替える

- `MUST`: 1枚に設定する主要ジョブは1つとする。
- `MUST`: 主要ジョブの達成に不要な情報を同じスライドへ詰め込まない。
- `MAY`: 主要ジョブを支える補助情報を含める。

### 5.2 タイトル

- 通常のスライドは、話題名ではなく、そのスライドで伝える結論または要点をタイトルにする。
- タイトルは30文字程度を目安とし、原則1行に収める。
- 通常のタイトル末尾に句点を付けない。
- 表紙、章扉、アジェンダ、問い、演習、付録、出典は例外とする。
- 問いは質問文、演習は実行してほしい行動をタイトルにできる。

例:

```text
避ける: 市場環境
推奨: 市場は3年間で約2倍に拡大している
```

## 6. 文字量と情報密度

次の数値は、タイトルと出典を除いた日本語の目安です。内容の正確性や理解を損なってまで数値へ合わせません。

| モード | 本文文字数の目安 | 箇条書き | 1項目の目安 |
|---|---:|---:|---:|
| `presented` | 80〜120文字 | 最大3項目 | 25〜35文字程度 |
| `standalone` | 140〜220文字 | 最大4項目 | 30〜50文字程度 |

`presented`は通常1つの説明まとまり、`standalone`は1〜2つの説明まとまりに抑えます。

情報が収まらない場合は、次の順序で調整します。

1. 重複と前置きを削る。
2. 表現を短くする。
3. 補足を付録または発表者ノートへ移す。
4. スライドを分割する。
5. 内容に適したレイアウトへ変更する。

最小文字サイズを下回る縮小で解決してはなりません。`standalone`では、付録へ移すと本文だけで理解できなくなる情報は、付録へ移さずスライドを分割します。

### 6.1 研修・教材の例外

- 演習の手順は3ステップ程度までとし、1ステップに1つの行動を書く。
- 演習には所要時間と完了条件を示す。
- 問いと答え・解説は、原則として別スライドにする。
- 長いケースは、状況説明と問いを分割できる。

## 7. 元資料、事実、外部情報

### 7.1 情報の優先順位

内容の根拠は、次の順序で扱います。

1. ユーザーが提供した元資料
2. ユーザーが明示した事実
3. 検証可能な外部情報
4. AIによる解釈または提案

- 元資料を単純に短縮するのではなく、対象者と目的に合わせて再構成する。
- 数値、日付、固有名詞は元資料に合わせ、推測で変更または補完しない。
- 事実と解釈を区別する。
- 元資料にない因果関係を、確定した事実として書かない。
- 外部情報は、ユーザーが求めた場合、または理解に不可欠で検証可能な場合だけ補足する。
- 外部情報を使用した場合は、追跡可能な出典を残す。

元資料同士に矛盾がある場合は、黙って一方を採用しません。結論に影響する場合は確認し、影響が小さい場合は断定を避けるか差異を明示します。新しい資料であることだけを、正しさの根拠にはしません。

### 7.2 出典の表示

出典は、次の2段階で残します。

1. 該当スライドに短い出典名と出典IDを表示する。
2. 資料末尾の出典ページに完全な情報を記載する。

例:

```text
出典: 総務省（2025）[S1]
```

出典が多い場合は、文字を極端に小さくせず、出典ページを複数に分けます。

- `standalone`: 該当スライドの短い出典と、末尾の完全な出典一覧を原則必須とする。PDFだけでも確認できる状態にする。
- `presented`: 重要な数値、引用、図表にはスライド上で出典を示し、完全なURLは発表者ノートまたは出典ページへ記載できる。

次の内容には原則として出典が必要です。

- 外部の統計、数値、引用、調査結果
- 外部資料をもとにしたグラフや表
- 外部の画像やイラスト
- 一般常識とは言い切れない事実や、時点によって変化する情報

独自の見出し、元資料の単純な整理、提案であることが明確な内容、一般的な接続表現には通常、出典を付けません。

## 8. 表紙、導入、終わり方

### 8.1 表紙

表紙には次を使用します。

| 項目 | 扱い |
|---|---|
| タイトル | 必須。元資料、対象者、利用目的から案を作り、制作前に確認する |
| 日付 | 必須。指定がなければ作成日を使用する。イベント日、報告基準日、版の日付が重要な場合はそちらを使う |
| サブタイトル | 任意。対象範囲、対象者、目的の理解に役立つ場合だけ使う |
| 作成者・組織名 | 任意。推測せず、提供されていない場合は表示しない |

表紙は最小限の要素で構成します。

### 8.2 アジェンダ

アジェンダは必須ではありません。次のいずれかに該当する場合に使用します。

- 3つ以上の章がある。
- 12枚程度以上の資料である。
- 研修で進行順を示す必要がある。
- `standalone`で参照性が高まる。

### 8.3 導入と結論

| 資料タイプ | 導入で示すこと | 結論で示すこと |
|---|---|---|
| `explain` | なぜこのテーマが重要か | 要点とその意味 |
| `proposal` | 課題または必要な判断 | 推奨案と求める行動・判断 |
| `report` | 結論または重要な変化 | 意味と次の対応 |
| `how_to` | 何ができるようになるか | 完了確認と次の行動 |
| `training` | 学習目標と到達状態 | 振り返りと次の練習 |

「ご清聴ありがとうございました」だけを結論にしません。資料の結論または次の行動を先に示します。出典や付録が末尾に続く場合も、物語上の結論はその前に置きます。

## 9. 枚数

- ユーザーが枚数を指定した場合は、その枚数を目標とする。
- 発表時間が指定された場合は、時間と説明量から枚数案を作る。
- どちらも指定されていない場合は、必要なスライドジョブから枚数を導く。
- 制作前の確認で、提案枚数を示す。
- 指定枚数では可読性を保てない場合は、枚数の調整を提案する。

指定がない場合の参考範囲は次のとおりです。

| 規模 | 参考枚数 |
|---|---:|
| 短い概要 | 5〜7枚 |
| 標準 | 8〜12枚 |
| 詳細 | 13〜20枚 |
| 研修・教材 | 時間、演習数、理解確認から算出 |

## 10. 発表者ノート

- `standalone`: 理解に必要な内容を発表者ノートだけに置かない。詳細な出典情報は記録できる。
- `presented`: 原則として簡潔な発表者ノートを用意する。

`presented`のノートには、必要に応じて次を2〜4項目程度で記載します。全文台本は、ユーザーが求めた場合だけ作成します。

- 話す要点
- スライド上で省略した補足
- 次のスライドへのつなぎ
- 目安時間
- 詳細な出典

`training`かつ`presented`の演習スライドには、次の進行用ノートを必ず用意します。

- 演習の目的
- 説明手順
- 所要時間
- 期待する回答または完了状態
- 参加者が詰まった場合の支援
- 振り返りの観点

発表者ノートの形式別の格納方法は`50_OUTPUTS.md`に従います。

## 11. 文章のトーン

文章は、明確、簡潔、中立、実用的にします。

- 対象者が使う言葉を優先する。
- 抽象語だけで済ませず、具体的な対象と行動を書く。
- 主語と行動を明確にする。
- 同じ概念には同じ用語を使う。
- 対象者が専門家でない場合は専門用語を説明する。
- 略語は初出時に正式名称を示す。
- 数値には単位、期間、比較対象を付ける。
- 箇条書きの文法形式をそろえる。
- 根拠のない最上級、過度に感情的な表現、宣伝的な表現を避ける。

`presented`では体言止めや短い断片を使用できますが、発表者が話す全文をスライドへ重複させません。`standalone`では主語、因果関係、前提を明示し、資料だけで意味が通る文にします。`training`では行動動詞を使い、演習の目的、手順、時間、完了条件を明確にします。

## 12. 本編、付録、除外

「削除すると、理解、判断または実行ができなくなるか」を、本編に残す基準とします。

### 12.1 本編に置く内容

- 結論
- 主要な根拠
- 理解に必要な前提
- 判断に影響する数値
- 重要なリスクと制約
- 求める行動
- 実行に不可欠な手順
- 研修の学習目標と中核の説明

判断に重要なリスクを付録だけに置いてはなりません。

### 12.2 付録に置ける内容

- 詳細な方法
- 完全なデータ表
- 補足グラフと追加例
- 用語集
- FAQ
- 詳細比較と代替案
- 予備の根拠
- 完全な出典一覧

`standalone`は付録を読まなくても本編の論旨が理解できるようにします。`presented`は想定質問への補足や予備データを付録へ置けます。

`training`では、演習の目的と指示を本編、進行支援を発表者ノート、追加演習や回答例の詳細を付録へ置けます。`standalone`の教材は、回答や解説へ資料内から到達できるようにします。

元資料の内容は、次のいずれかに分類します。

- 使用: 目的達成に必要な内容
- 付録: 中核ではないが参照価値がある内容
- 除外: 今回の目的に関係しない内容

ユーザーが明示的に含めるよう指定した内容は除外しません。量が多すぎる場合は、削除せず分割または別資料化を提案します。

## 13. 制作前の確認への反映

新規制作または資料全体の再構成では、`00_MASTER.md`に従って制作前の確認を行い、少なくとも次を確認内容へ含めます。

- コンテンツモード
- 資料タイプ
- 対象者と対象者プロファイル
- 利用目的と中心メッセージ
- 提案枚数または発表時間
- 表紙情報

安全に推定できる内容は質問を分けず、Claudeの提案として一括で提示します。
