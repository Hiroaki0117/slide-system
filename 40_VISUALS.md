---
spec_name: slide-system-visuals
version: 0.1.0
status: draft
updated: 2026-08-04
language: ja
default_theme: warm_clean
---

# Visual Specification

## 1. 目的

この仕様は、写真、イラスト、アイコン、図解、グラフ、表、スクリーンショットの選択、制作、配置、出典、検査を定義します。

すべてのページへ機械的に画像を追加するのではなく、内容の理解、根拠の提示、記憶、行動を助けるビジュアルを積極的に使用することを目的とします。

内容と出典の事実性は`10_CONTENT.md`、テーマと可読性は`20_DESIGN.md`、配置と収容量は`30_LAYOUTS.md`、形式別の実装と検査は`50_OUTPUTS.md`に従います。

## 2. ビジュアルの役割

ビジュアルは次の5つの役割から選びます。

| 役割 | 目的 | 主な素材 |
|---|---|---|
| `evidence` | 事実、数値、傾向を示す | グラフ、表、重要数値 |
| `explain` | 関係、構造、流れ、操作を説明する | 図解、フロー、スクリーンショット |
| `context` | 人物、場所、製品、状況、具体例を伝える | 写真、イラスト、事例画像 |
| `decoration` | 視線誘導やテーマの印象を補助する | 円、線、淡い背景図形 |
| `none` | 文字だけで最も明確に伝える | ビジュアルを使用しない |

- `MUST`: スライド計画時に、各ページのビジュアル役割を1つ以上設定する。
- `MUST`: ビジュアルを使わないことが最適な場合は、`none`を選択できる。
- `MUST`: 装飾を、根拠や説明を担うビジュアルとして扱わない。
- `SHOULD`: ビジュアルを追加する前に、その素材が理解、根拠、記憶、行動のどれを助けるか説明できるようにする。

## 3. 使用量とバランス

- `MUST`: すべてのページに写真やイラストを配置することを必須としない。
- `MUST`: 内容に不要なビジュアルを、空白を埋める目的だけで追加しない。
- `SHOULD`: 各ページで、意味のあるビジュアルを使用できないか積極的に検討する。
- `SHOULD`: 1ページにつき、主役となるビジュアルは原則1つにする。
- `MAY`: 比較、手順、複数事例など、複数素材そのものに意味がある場合は、複数のビジュアルを使用できる。
- `MAY`: 装飾図形は、主役となるビジュアルの数に含めない。

文字中心のページが4ページ以上続く場合は、意味のあるビジュアルが不足していないか見直します。連続していること自体は違反とせず、内容に最適であるかを確認するためのレビュー信号として扱います。

## 4. 素材の優先順位

ビジュアル素材は、次の順序を基本とします。

1. 依頼者が使用を明示した素材
2. 依頼者から提供された写真、画面、データ、図表
3. 元資料から制作するグラフ、表、図解
4. 利用条件と出典を確認できる外部素材
5. AI生成の写真またはイラスト
6. `warm_clean`の装飾図形

### 4.1 依頼者指定素材

- `MUST`: 依頼者から使用を明示された写真または素材は使用する。
- `MUST`: 類似写真やAI生成画像へ無断で置き換えない。
- `MUST`: 重要部分を切る、縦横比を崩す、意味を変える加工を行わない。
- `MUST`: 提供されたクレジット、出典、利用条件を引き継ぐ。
- `MUST`: 解像度や権利に問題がある場合は、隠さず伝える。
- `SHOULD`: 指定素材と`warm_clean`が合わない場合は、素材を過度に加工せず、余白、背景、文字色など周囲のデザインを調整する。

## 5. ビジュアルタイプの選択

| 伝える内容 | 第一候補 |
|---|---|
| 数値、比較、推移、構成比 | グラフまたは表 |
| 手順、因果、関係、構造 | 図解またはフロー |
| ソフトウェアの操作や画面状態 | スクリーンショット |
| 実在する人物、場所、製品、出来事 | 写真 |
| 抽象概念、未来像、感情、比喩 | イラスト |
| 雰囲気や視線誘導のみ | テーマ固有の装飾 |

- `SHOULD`: 同じ内容を文章、図、写真で重複して説明せず、最も理解しやすい形式を選ぶ。
- `SHOULD`: 正確な数値確認が必要なら表、傾向の理解が必要ならグラフを優先する。
- `SHOULD`: 実在性が重要な内容を、根拠のないAI生成写真で置き換えない。

## 6. 写真

### 6.1 共通ルール

- `MUST`: 写真が伝える対象と、スライドの主要メッセージを一致させる。
- `MUST`: 実在人物、場所、製品、出来事について、誤認を招く写真を使用しない。
- `MUST`: 同じ写真を、背景を除き原則として複数ページで使い回さない。
- `MUST`: 人物の顔、製品、文字、数値などの重要部分を意図せず切らない。
- `MUST`: 縦横比を維持し、不自然に引き伸ばさない。
- `SHOULD`: タイトルや本文を写真へ重ねる場合は、十分な余白とコントラストを確保する。
- `SHOULD`: 意味の薄い定番ストック写真より、内容に固有の場面や具体物を優先する。

### 6.2 `warm_clean`の写真スタイル

- 自然光を感じる明るい写真を優先する。
- 彩度は低〜中程度とする。
- 背景が整理され、適度な余白がある構図を優先する。
- 強いフィルター、過剰な色調補正、極端なHDR表現を避ける。
- 基本は長方形または控えめな角丸で配置する。
- 依頼者提供写真へ`warm_clean`の色調を無理に適用しない。

## 7. イラスト

### 7.1 共通ルール

- `MUST`: イラストが事実の写真または実在する証拠であるように見せない。
- `MUST`: AI生成イラスト内の文字を、本文や正確なラベルとして使用しない。
- `MUST`: 1資料内で画風、線、色、人物表現の方向性を揃える。
- `SHOULD`: 抽象概念、未来像、感情、比喩など、写真や図解では伝えにくい内容へ使用する。
- `SHOULD`: 細部を増やしすぎず、縮小表示でも主題が分かる構図にする。

### 7.2 `warm_clean`のイラストスタイル

- フラットなエディトリアルイラストを基本とする。
- シンプルで、わずかに有機的な形を使用する。
- コーラル、ミント、チャコールを中心とした限定色を使用する。
- 子ども向けに見える過度なデフォルメを避ける。
- 3D、光沢、写実的CG、過度に緻密な表現を避ける。
- 背景は透過、または`#FFF8F4`と自然につながる色を優先する。

## 8. アイコン

- `MUST`: 重要な意味をアイコンだけに任せず、文字またはラベルを併記する。
- `MUST`: 同じ資料で、線画、塗り、絵文字、3Dなど異なるスタイルを混在させない。
- `MUST`: アイコンだけの操作案内やナビゲーションを作らない。
- `SHOULD`: 情報の分類、短い補助説明、視線誘導に使用する。
- `SHOULD`: 1つの分類または概念に、原則1つのアイコンを対応させる。
- `SHOULD`: 空白を埋めるためだけのアイコンを配置しない。

`warm_clean`では、シンプルな線画または最小限の単色アイコンを使用します。チャコールを基本色とし、コーラルを主要な強調、ミントを補助背景として使用できます。

## 9. 図解

- `MUST`: 文章よりも、関係、流れ、構造を明確にできる場合だけ使用する。
- `MUST`: 1つの図解で伝える中心メッセージを1つにする。
- `MUST`: 矢印の方向と線の意味を図の中で統一する。
- `MUST`: 線をノードや文字の上へ重ねず、不必要な交差を避ける。
- `MUST`: 色だけに意味を持たせず、ラベル、形、線種、配置を併用する。
- `SHOULD`: 初期案では要素数を5〜7個以内にする。
- `SHOULD`: ラベルを短くし、階層と読み順を明確にする。
- `SHOULD`: アイコンを並べただけの構成を、無理に図解として扱わない。
- `SHOULD`: 複雑な図は複数ページに分割するか、詳細を付録へ移す。

要素数は試作後に見直す暫定値です。単純な流れや関係は後から編集できる図形で制作し、複雑なネットワークは専用の図解方法を使用します。見た目を重視する概念図は、編集可能性との優先順位を確認したうえでイラストとして制作できます。

## 10. グラフ

### 10.1 選択

| 目的 | 基本形式 |
|---|---|
| 項目間の比較 | 棒グラフ |
| 時系列の推移 | 折れ線グラフ |
| 全体に対する構成 | 積み上げ棒グラフ |
| 2変数の関係 | 散布図 |
| 少数項目の単純な構成比 | 円グラフを使用可能 |

- `MUST`: 制作前に「このグラフで何を伝えるか」を1文で定める。
- `MUST`: 数値、対象、期間、単位、出典を確認し、推測や創作で補わない。
- `MUST`: グラフの形式によって差を誇張または過小評価させない。
- `MUST`: 棒グラフは原則としてゼロを起点にする。
- `MUST`: 折れ線などでゼロ以外を起点にする場合は、軸の範囲を明確にする。
- `MUST`: 系列を色だけで区別しない。
- `SHOULD`: 3D表現、不要な補助線、過剰な凡例、安易な二軸グラフを避ける。
- `SHOULD`: 凡例より直接ラベルを優先する。
- `SHOULD`: 短い結論または注釈を添え、データの意味を示す。
- `SHOULD`: 円グラフは少数項目に限定し、通常は棒グラフとの比較を検討する。

`warm_clean`では、注目させる系列をコーラル、その他を中立色とします。ミントは第二の意味を持つ系列または補助情報へ限定します。系列数が多い場合はテーマ色だけで対応せず、明度、線種、記号、直接ラベルを組み合わせます。

## 11. 表

- `MUST`: 正確な数値の確認または複数項目の照合が必要な場合に使用する。
- `MUST`: 単位、基準日、対象、出典を必要に応じて明記する。
- `MUST`: 読み切れない文字サイズへ縮小しない。
- `SHOULD`: 大まかな傾向を伝えるだけなら、表よりグラフを優先する。
- `SHOULD`: 1つの表につき比較目的を1つに絞る。
- `SHOULD`: 重要な行、列、セルだけを強調する。
- `SHOULD`: 罫線を最小限にし、見出し、整列、余白で構造を示す。
- `SHOULD`: 長い文章をセルへ詰め込まない。
- `SHOULD`: 6列×8行程度を初期目安とし、超える場合は分割または付録への移動を検討する。

行列数は試作後に調整する暫定値です。

## 12. スクリーンショット

- `MUST`: 操作手順または画面状態を正確に説明する場合に使用する。
- `MUST`: 個人情報、通知、アカウント名、機密情報が含まれていないか確認する。
- `MUST`: 画面内の文字が読めない大きさになる場合は、該当箇所を拡大または別ページに分ける。
- `MUST`: 説明対象となるUIや文字を、注釈や切り抜きで隠さない。
- `SHOULD`: 説明に不要なブラウザ枠、デスクトップ、余分な余白を除く。
- `SHOULD`: 操作手順では、番号と短い説明を対応させる。
- `SHOULD`: 複数画面を比較する場合は、縮尺と切り抜き範囲を可能な限り揃える。

## 13. 切り抜きと注釈

### 13.1 切り抜き

- `MUST`: 顔、視線の先、製品、数値、UIなど、説明上重要な部分を切らない。
- `MUST`: 元画像の縦横比を維持する。
- `MUST`: 依頼者指定画像の意図を変える切り抜きを行わない。
- `SHOULD`: 人物写真では顔や視線の先に適度な余白を残す。
- `SHOULD`: 文字を重ねる場合は、文字を置くための余白がある構図を優先する。
- `SHOULD`: 判断が難しい場合は、全体を残すことを優先する。

### 13.2 注釈

- `MUST`: 矢印の先端を対象へ正確に合わせる。
- `MUST`: 注釈で元の重要情報を隠さない。
- `MUST`: 色だけでなく、番号、枠線、ラベルのいずれかを併用する。
- `SHOULD`: 枠線、矢印、番号、短いラベルを基本とする。
- `SHOULD`: 1枚の画像に注釈を載せすぎず、複雑な場合はページを分ける。
- `SHOULD`: `warm_clean`ではコーラルを主な注釈色にする。
- `SHOULD`: 強調箇所以外を暗くする場合も、内容を確認できる明るさを残す。

## 14. 画質

- `MUST`: スライド上でぼやける、文字が潰れる、縦横比が崩れる画像を使用しない。
- `MUST`: 小さい画像を無理に拡大しない。
- `MUST`: 透かしや不要な文字が入った画像を使用しない。
- `MUST`: 仮画像を完成画像と区別し、最終出力前に残っていないか確認する。
- `MUST`: ロゴ、図表、スクリーンショットの細部を、実際の出力サイズで確認する。
- `MUST`: 依頼者指定素材の品質が不足する場合は、問題と影響を明示する。

## 15. 出典と利用条件

### 15.1 記録

- `MUST`: 外部から取得した画像、アイコン、データ、図表の取得元を記録する。
- `MUST`: 記録には、可能な範囲で作者または提供元、ページ名、URL、確認日、利用条件を含める。
- `MUST`: 元画像を加工した場合も、元の出典を引き継ぐ。
- `MUST`: 依頼者提供素材は「依頼者提供」など、提供元を識別できる形で記録する。
- `MUST`: AI生成素材は、AI生成であることと使用した生成手段を記録する。
- `SHOULD`: 使用ページの左下に短い出典を載せ、詳細URLなどは出典ページまたはノートにまとめる。

### 15.2 利用条件

- `MUST`: 利用条件を確認できない外部素材を使用しない。
- `MUST`: 検索結果に表示された画像を、そのまま転載しない。
- `MUST`: 商用利用、加工、クレジット表記など、用途に関係する条件を確認する。
- `MUST`: ロゴとブランド素材は、公式配布素材とガイドラインを優先する。
- `MUST`: 引用として使用する場合も、必要な範囲に限定し出典を示す。
- `MUST`: 依頼者指定素材に明らかな権利上の懸念がある場合は、制作前または納品前に確認を求める。
- `SHOULD`: 条件を確認できない場合は、自作図解、利用条件が明確な素材、AI生成素材などへの変更を提案する。

## 16. テーマ固有の視覚設定

すべてのテーマは、共通のビジュアルルールに加えて次を定義します。

- 写真の明るさ、彩度、構図、切り抜き、加工方針
- イラストの画風、線、形、色数、背景、避ける表現
- アイコンの線画または塗り、線幅、色、ラベルとの関係
- 図解のノード、線、矢印、強調の表現
- グラフの系列色、中立色、強調色、補助線、ラベル
- 表の見出し、罫線、強調セルの表現
- スクリーンショットの枠、角丸、注釈色

`warm_clean`の設定は、本仕様の各素材種別に記載した値を使用します。テーマ固有設定は、共通の事実性、可読性、アクセシビリティ、出典、利用条件を上書きできません。

## 17. 将来のテーマ追加

新しいテーマを追加する場合は、`20_DESIGN.md`のテーマ拡張契約に従い、前節の視覚設定をすべて定義します。

- `MUST`: 資料タイプだけを理由にテーマを自動選択しない。
- `MUST`: `10_CONTENT.md`の内容ルールと`30_LAYOUTS.md`の収容量を上書きしない。
- `MUST`: 表紙、文章中心、写真またはイラスト、図解、データ、スクリーンショットの代表ページで検証する。
- `MUST`: HTML/PDFとPPTX/PDFの両方で再現性を確認する。
- `MUST`: 指定テーマが存在しない、必要項目が不足する、または利用できない場合は`warm_clean`へ戻し、その旨を伝える。

第2テーマを実際に追加するまでは、`themes/`ディレクトリを作成しません。追加時には`themes/THEME_TEMPLATE.md`と、`themes/warm_clean.md`を含むテーマ別ファイルへ分離できます。

## 18. 検査

### 18.1 ページ単位

- ビジュアルを使う目的が説明できる。
- 主役となるビジュアルが明確である。
- 装飾が本文より目立っていない。
- 画像、文字、注釈に意図しない重なりがない。
- 切り抜きによって重要情報が欠けていない。
- 小さな文字と数値を無理なく読める。
- 色だけに意味を依存していない。
- 必要な出典が表示または記録されている。
- 依頼者指定素材が正しく使用されている。

### 18.2 資料全体

- 写真、イラスト、アイコンのスタイルが統一されている。
- コーラルとミントの役割がページごとに変わっていない。
- ビジュアルのないページが続く場合、内容上の理由がある。
- 同じ画像を安易に使い回していない。
- 装飾目的の図形が増えすぎていない。
- `warm_clean`の明るさ、余白、親しみやすさを維持している。
- 表紙、本文、データ、操作説明で品質に大きな差がない。

### 18.3 出力後

- 画像が欠落していない。
- 解像度が不足していない。
- 縦横比が崩れていない。
- 文字と注釈の位置がずれていない。
- ページ端で画像、注釈、出典が切れていない。
- PDF化によって色や透明度が不自然になっていない。

## 19. 試作後に見直す項目

最初の試作では、少なくとも次のページを含めます。

1. 写真を使ったページ
2. イラストを使ったページ
3. アイコンを使ったページ
4. 図解またはプロセスのページ
5. グラフまたは表のページ
6. スクリーンショットに注釈したページ
7. ビジュアルを使わない文章中心のページ

各ページを`採用`、`要調整`、`不採用`で判定し、次の暫定値を見直します。

- 1ページにつき主役となるビジュアルを1つとする目安
- 文字中心のページが4ページ続いた場合の見直し
- 図解の要素数5〜7個
- 表の6列×8行
- 写真、イラスト、アイコン、装飾の使用量
- `warm_clean`の写真、イラスト、注釈、グラフの表現

見直しでは、好みだけでなく、理解しやすさ、事実性、可読性、再現性、編集可能性、形式変換後の品質を確認します。
