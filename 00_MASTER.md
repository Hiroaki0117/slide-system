---
spec_name: slide-system-master
version: 0.6.0
status: draft
updated: 2026-08-04
language: ja
---

# Slide System Master Specification

## 1. 目的

この仕様は、Claudeのプロジェクト内で、短い依頼から一貫した品質のスライドを作成するための共通工程を定義します。

目標は、ユーザーに複雑なプロンプトを要求することではありません。対象者、利用目的、元資料などの最低限の入力を受け取り、登録された仕様に沿って構成、制作、検査、納品まで進めることです。

## 2. 適用範囲

次の作業に適用します。

- 新規スライドの作成
- 既存スライドの修正
- スライド構成案の作成
- HTML、PPTX、PDFへの出力または変換
- スライドの品質評価

単なる文章要約を依頼された場合は、スライド形式を勝手に選択しません。依頼にスライド、プレゼン、資料、デッキなどの意図がある場合に本仕様を適用します。

## 3. 用語

- `MUST`: 必ず守る。例外は、ユーザーが意図的かつ明確に変更を指示した場合に限る。
- `SHOULD`: 原則として守る。内容や利用目的に合理的な理由がある場合は変更できる。
- `MAY`: 必要な場合に使用できる。
- `出力プロファイル`: 主成果物と派生成果物、形式別の生成・検査方法をまとめた定義。
- `主成果物`: 編集や再生成の基準となるファイル。
- `派生成果物`: 主成果物から変換して作るPDFなどのファイル。

## 4. 仕様ファイル

| ファイル | 役割 | 現在の状態 |
|---|---|---|
| `00_MASTER.md` | 制作工程、優先順位、既定値 | 有効 |
| `10_CONTENT.md` | 構成、文章、情報量 | 有効 |
| `20_DESIGN.md` | 色、文字、余白、トンマナ | 有効 |
| `30_LAYOUTS.md` | レイアウトタイプと選択条件 | 有効 |
| `40_VISUALS.md` | 写真、イラスト、図解、グラフ | 有効 |
| `50_OUTPUTS.md` | 出力プロファイルと形式別要件 | 有効 |
| `60_QA.md` | 着手前確認、検査、修正ループ、完成判定 | 有効 |

登録されていない仕様書の内容を推測して、存在するルールとして扱ってはなりません。未作成の領域は、このファイルの共通原則とユーザーの依頼に基づいて判断します。

## 5. 仕様の優先順位

判断が競合した場合は、次の順序で扱います。

1. 事実性、安全性、実際の生成能力に関する制約
2. 今回の依頼で明示されたテーマ、対象者、利用目的、元資料、出力形式
3. このファイルの`MUST`ルール
4. 個別仕様書の`MUST`ルール
5. 個別仕様書の`SHOULD`ルールと既定値
6. 作例

ユーザーの指示が`MUST`ルールと明確に異なり、意図的な変更だと判断できる場合は、今回に限る例外として扱います。意図が不明な場合は仕様を優先し、結果を大きく変えるときだけ確認します。

## 6. 毎回の入力

制作時に使用する情報は次のとおりです。

| 項目 | 重要度 | 未指定時の扱い |
|---|---|---|
| テーマまたは元資料 | 必須 | 推測せず確認する |
| 対象者 | 推奨 | 一般的な対象者。対象者プロファイルは`general` |
| 利用目的 | 推奨 | 内容を理解してもらう説明資料 |
| 中心メッセージ | 任意 | 元資料と目的から導く |
| コンテンツモード | 任意 | `standalone` |
| 資料タイプ | 任意 | 利用目的から推定する。判断できない場合は`explain` |
| デザインテーマ | 任意 | `warm_clean`。テンプレートまたはブランド指定がある場合は、その指定を優先する |
| 枚数 | 任意 | 必要なスライドの主要ジョブから提案する。標準は8〜12枚を参考にする |
| 出力形式 | 任意 | `50_OUTPUTS.md`の既定プロファイル |
| 言語 | 任意 | 日本語 |
| 画面比率 | 任意 | 16:9 |
| 表紙タイトル | 必須 | テーマ、元資料、利用目的から案を作成し、制作前に確認する |
| 表紙日付 | 必須 | 作成日を使用する |
| 表紙サブタイトル | 任意 | 内容理解に役立つ場合のみ案を作成する |
| 表紙の作成者・組織名 | 任意 | 推測せず、提供がなければ表示しない |

個別の質問は、回答によって成果物が大きく変わる場合だけ行います。既定値や安全な推定で進められる項目は一問ずつ質問せず、制作条件としてまとめて確認します。

## 7. 制作工程

### 7.1 依頼の解釈

次を特定します。

- 誰に見せるのか
- 何のために見せるのか
- 見た人に何を理解、判断、選択、実行してほしいのか
- 最も伝えるべきことは何か
- どの元資料と事実を使用できるか

制作目的を内部的に次の一文へ整理します。

```text
この資料を見た［対象者］が、［中心メッセージ］を根拠として、［到達してほしい状態］になる。
```

この内部整理を、そのままスライド上に表示する必要はありません。

### 7.2 出力プロファイルの決定

`50_OUTPUTS.md`に従い、主成果物と派生成果物を決めます。

出力形式が明示されている場合は、その指定を使用します。指定形式を生成できない場合は、無断で別形式へ変更しません。

### 7.3 制作条件の確認

新しいスライドの制作または資料全体の再構成を開始する前に、依頼内容、仕様書、既定値から制作条件を整理し、ユーザーへ1回の確認を求めます。軽微な修正、変換、評価だけを依頼された場合は、結果に影響する条件が不足していない限り、この確認を省略できます。

確認内容には、該当する範囲で次を含めます。

- 表紙タイトル
- 表紙日付
- 表紙サブタイトルの有無と内容
- 表紙の作成者・組織名の有無と内容
- 対象者
- 利用目的
- 中心メッセージ
- コンテンツモード
- 資料タイプ
- デザインテーマ、テンプレート、ブランド指定
- 枚数または発表時間
- 出力プロファイル
- 言語
- 画面比率

表紙にはタイトルと日付を必ず含めます。タイトルはテーマ、元資料、対象者、利用目的から案を作成します。日付の指定がなければ作成日を使用します。サブタイトルは内容理解に役立つ場合だけ提案します。作成者・組織名は推測せず、ユーザーから提供されていない場合は表示しません。

構造化された質問または選択肢を提示できる場合は使用します。利用できない場合は、通常のチャットで制作条件を簡潔に提示します。原則として、次の2つの選択肢で確認します。

```text
- この内容で進める
- 内容を修正する
```

ユーザーが修正を選んだ場合は、変更箇所だけを確認します。ユーザーが事前確認を不要と明示した場合は、安全に適用できる既定値を使用して進めます。

制作条件と出力プロファイルを整理した後、本制作へ入る前に`60_QA.md`のゲート0を実行します。元資料、指定素材、利用条件、生成・変換・検査能力に問題があり、成果物へ影響する場合は、推奨案と代替案を提示してユーザーの判断を求めます。

### 7.4 ストーリー設計

`10_CONTENT.md`に従い、コンテンツモード、資料タイプ、対象者プロファイルを決め、目的に合う流れを選びます。

アジェンダを並べただけの構成にせず、前のスライドが次のスライドを必要とする流れにします。

### 7.5 スライド計画

制作前に、各スライドについて少なくとも次を決めます。

- スライド番号
- 主要ジョブ
- 主要メッセージまたは求める行動
- 仮タイトル
- 使用する根拠または元資料
- レイアウトタイプ
- ビジュアル役割（`evidence`、`explain`、`context`、`decoration`、`none`）
- 前後のスライドとのつながり

計画は制作のための内部情報です。ユーザーが求めていない限り、完成スライド上に制作メモを表示しません。

### 7.6 制作

- `MUST`: `10_CONTENT.md`に従い、各スライドに主要ジョブを1つ設定する。
- `MUST`: 根拠のない事実、数値、引用、人物、事例を作らない。
- `MUST`: 内容は`10_CONTENT.md`、デザインは`20_DESIGN.md`、レイアウトは`30_LAYOUTS.md`、視覚素材は`40_VISUALS.md`、出力は`50_OUTPUTS.md`の登録済み仕様に従う。

未作成の個別仕様書に属する事項は、このファイルの共通原則、ユーザーの依頼、内容上の必要性から判断します。

### 7.7 検査と修正

完成前に、登録済みの`60_QA.md`と`50_OUTPUTS.md`の形式別検査を実行します。

少なくとも次を確認します。

- 目的と対象者に合っているか
- 資料全体の流れがつながっているか
- 各スライドの主要ジョブと主要メッセージが明確か
- 文字、画像、図形のはみ出し、重なり、意図しない改行がないか
- 元資料の内容と一致しているか
- 数値、引用、外部素材の出典を追跡できるか
- 指定された形式のファイルが実際に生成されているか

問題が見つかった場合は、`60_QA.md`の修正ループに従って修正、再生成、再検査を行います。すべての必須項目が合格するまで完成として提出しません。

## 8. 出典と事実性

出典表示、情報の優先順位、元資料の矛盾、外部情報の扱いは`10_CONTENT.md`に従います。

- `MUST`: 情報が不足している場合は、もっともらしい内容を作って穴埋めしない。
- `MUST`: 外部情報を追加した場合は、出典を追跡できる形で残す。
- `MUST`: 事実とAIによる解釈または提案を混同しない。

## 9. 完成の定義

次のすべてを満たした状態を完成とします。

1. 依頼された内容を満たしている。
2. 適用対象の仕様書に準拠している。
3. `60_QA.md`のすべての必須項目が`PASS`または理由を記録した`N/A`である。
4. 指定された成果物が実際に生成されている。
5. 残っている`WARN`、任意検査の`NOT_VERIFIED`、既知の制約が隠さず明示されている。

構成案、ソースコード、プレビューだけを作成した状態を、最終成果物の完成とは扱いません。

## 10. 失敗時の扱い

- 必須仕様書へアクセスできない場合は、利用できないファイルと影響を伝えます。
- 指定形式を生成できない場合は、生成できなかった形式と理由を伝えます。
- 代替形式を提案できますが、ユーザーの同意なく完成形式を変更しません。
- 変換や検査が未実施の場合は、その状態を明示します。
- 生成していないファイルを、生成済みと報告しません。

## 11. 仕様の保守

このファイルは、制作工程と仕様間の関係に限定して短く保ちます。個別の文字数、色、余白、レイアウト、画像量、出力実装、検査項目は、それぞれの個別仕様書へ記載します。
