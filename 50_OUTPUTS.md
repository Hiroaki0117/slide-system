---
spec_name: slide-system-outputs
version: 0.1.0
status: draft
updated: 2026-08-04
language: ja
default_profile: html_pdf
---

# Output Specification

## 1. 目的

この仕様は、スライドの出力形式、主成果物、派生成果物、形式別の生成条件と検査条件を定義します。

HTMLとPPTXは、単なる拡張子の違いではありません。編集性、実装方法、変換方法、検査方法が異なるため、別の出力プロファイルとして扱います。

## 2. 共通原則

- `MUST`: 出力形式ごとに、編集や再生成の基準となる主成果物を1つ決める。
- `MUST`: PDFは、選択した主成果物から生成する派生成果物として扱う。
- `MUST`: 主成果物とPDFの内容、ページ数、スライド順を一致させる。
- `MUST`: 実際に生成、変換、保存できたファイルだけを完成済みとして報告する。
- `MUST`: 指定された出力形式を、ユーザーに断らず別形式へ変更しない。
- `MUST`: 生成後に形式別の検査を行う。
- `SHOULD`: 画面比率は16:9とする。ユーザーが別の比率を指定した場合は、その指定を優先する。
- `SHOULD`: PDFを直接デザインせず、主成果物から再現可能な方法で生成する。

## 3. 出力プロファイル

| プロファイル | 主成果物 | 派生成果物 | 主な用途 |
|---|---|---|---|
| `html_pdf` | HTML | PDF | ブラウザ閲覧、共有、配布 |
| `pptx_pdf` | PPTX | PDF | PowerPointでの編集、社内資料 |
| `html_only` | HTML | なし | Web公開、試作、PDF不要 |
| `pptx_only` | PPTX | なし | 編集可能なファイルの受け渡し |

既定の出力プロファイルは`html_pdf`です。この既定値は初期仮説であり、実運用テスト後に見直します。

## 4. プロファイルの選択

| ユーザーの指定または利用目的 | 選択するプロファイル |
|---|---|
| HTML、Web、ブラウザで閲覧したい | `html_pdf` |
| HTMLだけ欲しい | `html_only` |
| PowerPoint、パワポ、編集可能な資料が欲しい | `pptx_pdf` |
| PPTXだけ欲しい | `pptx_only` |
| PDFだけ欲しい、編集の必要はない | `html_pdf`を使用し、PDFを納品対象とする |
| PDFだけ欲しい、PowerPointでの編集も想定する | `pptx_pdf`を使用し、PDFを納品対象とする |
| 指定がない | `html_pdf` |

PDFだけを指定され、利用目的から主成果物を判断できない場合は、既定の`html_pdf`を使用します。判断の違いが成果物に重大な影響を与える場合だけ、ユーザーに確認します。

## 5. 共通の出力条件

### 5.1 ファイル名

- `SHOULD`: 半角英数字とハイフンを基本とする。
- `SHOULD`: 同じ資料の主成果物と派生成果物は、拡張子を除いて同じ名前にする。
- `MAY`: ユーザーがファイル名を指定した場合は、その指定を使用する。

例:

```text
claude-slide-system.html
claude-slide-system.pdf
```

### 5.2 内容の一致

- スライド数とPDFのページ数を一致させます。
- スライドの順序、タイトル、本文、図表、出典を一致させます。
- 変換時に消える操作UIや編集用ガイドは、内容の不一致とは扱いません。

### 5.3 出典

- 外部の事実、数値、引用、画像には追跡可能な出典を残します。
- 形式がノート欄を持つ場合は、詳細なURLや出典情報をノート欄へ記録できます。
- 表示が必要な出典は、スライド内または出典スライドに記載します。

## 6. `html_pdf`プロファイル

### 6.1 主成果物

- 主成果物はHTMLです。
- `SHOULD`: 可能な限り単一のHTMLファイルとして作成する。
- `SHOULD`: 外部ライブラリ、外部フォント、ネットワーク接続への依存を最小限にする。
- `MUST`: 16:9のスライド単位で表示する。
- `MUST`: 前後のスライドへ移動できる。
- `SHOULD`: 現在のページ番号と総ページ数を表示する。
- `MUST`: PDF出力時に、1スライドを1ページとして扱える印刷設定を持つ。
- `MUST`: PDF出力時に、ナビゲーションや操作説明などの不要なUIを非表示にする。

### 6.2 PDF

- HTMLからPDFを生成します。
- 背景色、背景画像、文字色を保持します。
- スライド外の余白やブラウザUIを含めません。
- ページ順とページ数をHTMLに一致させます。

### 6.3 検査

少なくとも次を確認します。

- HTMLがブラウザで開ける。
- 最初から最後までスライド移動ができる。
- ページ番号が正しく更新される。
- すべてのスライドが表示領域内に収まる。
- 文字、画像、図表に意図しないはみ出しや欠けがない。
- PDFのページ数がHTMLのスライド数と一致する。
- PDFで背景、文字、画像が欠落していない。

`MAY`: 利用場面に必要な場合は、デスクトップとモバイルの両方で表示を確認します。

## 7. `pptx_pdf`プロファイル

### 7.1 主成果物

- 主成果物はPPTXです。
- `MUST`: 文字を可能な限り編集可能なテキストとして保持する。
- `SHOULD`: 表、グラフ、単純な図形を可能な範囲で編集可能にする。
- `MUST`: 本文を収める目的で、仕様の最小文字サイズを下回らない。
- `MUST`: 代替フォントや環境差による改行崩れを考慮する。
- `SHOULD`: 外部の事実、数値、画像の詳細な出典をノート欄に記録する。

### 7.2 PDF

- PPTXからPDFを生成します。
- PPTXとPDFのスライド順、ページ数、内容を一致させます。
- 変換によるフォント置換、改行変化、図形欠落がないか確認します。

### 7.3 検査

少なくとも次を確認します。

- PPTXが正常に開ける。
- すべてのスライドを画像として表示し、1枚ずつ確認する。
- 文字、画像、図形の重なり、欠け、意図しない改行がない。
- 未解決のプレースホルダーが残っていない。
- グラフ、表、数値が元資料と一致している。
- PDFのページ数がPPTXのスライド数と一致する。
- PDF変換後もレイアウトと内容が維持されている。

## 8. 単一形式のプロファイル

### 8.1 `html_only`

`html_pdf`のHTML要件とHTML検査を適用します。PDFの生成とPDF検査は行いません。

### 8.2 `pptx_only`

`pptx_pdf`のPPTX要件とPPTX検査を適用します。PDFの生成とPDF検査は行いません。

## 9. 生成能力の確認

制作を開始する前に、指定された形式を生成、変換、検査できる手段が利用可能か確認します。

- HTMLを作成できてもPDFへ変換できない場合、HTMLだけを完成済みとして報告します。
- PPTXを作成できてもPDFへ変換できない場合、PPTXだけを完成済みとして報告します。
- プレビューや構成案だけの場合、HTMLまたはPPTXの完成とは報告しません。
- 未検査のファイルは、未検査であることを明示します。

利用できない形式がある場合は、次を伝えます。

1. 生成できなかった形式
2. 生成できなかった理由
3. 実際に生成できた成果物
4. 利用可能な代替案

代替形式の提案はできますが、ユーザーの同意なく納品形式を変更しません。

## 10. 納品時の報告

納品時は、少なくとも次を簡潔に伝えます。

- 選択した出力プロファイル
- 実際に生成したファイル
- 実施した検査
- 未完了事項または既知の制約

仕様書や内部の制作計画を毎回すべて説明する必要はありません。
