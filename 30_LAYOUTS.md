---
spec_name: slide-system-layouts
version: 0.1.2
status: draft
updated: 2026-08-06
language: ja
layout_strategy: hybrid
base_family_count: 12
initial_layout_count: 16
---

# Layout Specification

## 1. 目的

この仕様は、スライドのレイアウトファミリー、選択条件、必要領域、収容量、バリエーション、連続使用、試作後の検証方法を定義します。

内容に適した配置を安定して選びながら、文章量や画像比率の違いへ対応できる柔軟性を残すことを目的とします。

構成と文章量は`10_CONTENT.md`、色、文字、余白、固定要素は`20_DESIGN.md`、視覚素材の選択と表現は`40_VISUALS.md`、形式別の実装と検査は`50_OUTPUTS.md`に従います。

## 2. レイアウト戦略

完全固定でも完全自由でもなく、少数の基本ファミリーと限定的なバリエーションを使用するハイブリッド方式とします。

- `MUST`: 制作前に、各スライドの主要ジョブと主要素材からレイアウトファミリーを選ぶ。
- `MUST`: 適合するファミリーがある場合は、見た目の変化だけを目的に新しい配置を即興で作らない。
- `MUST`: 内容が収まらない場合は、最小文字サイズを下回って詰め込まない。
- `SHOULD`: ファミリーが定める領域と情報階層を維持し、その範囲で面積比率や位置を調整する。
- `SHOULD`: 左右反転や比率変更を無制限に別レイアウトとして登録しない。
- `MAY`: 試作で再利用性が確認できた配置を、正式なバリエーションとして追加できる。

レイアウトはデザインテーマとは分離します。同じレイアウトへ`warm_clean`以外のテーマを適用できます。

## 3. 共通領域

通常スライドは、必要に応じて次の領域を持ちます。

- タイトル領域
- 主要コンテンツ領域
- 補助コンテンツ領域
- ビジュアルまたは図表領域
- 出典領域
- ページ番号領域

- `MUST`: タイトル、主要コンテンツ、出典、ページ番号を意図せず重ねない。
- `MUST`: 出典とページ番号は`20_DESIGN.md`の固定位置と安全余白を維持する。
- `MUST`: 主要コンテンツ領域を、装飾だけで埋めない。
- `SHOULD`: 左右の安全余白は均等にし、内部構成は内容に応じて非対称にできる。
- `SHOULD`: 1行を想定したタイトルは1行で表示する。
- `MAY`: 表紙、章扉、単一メッセージでは、通常のタイトル領域とコンテンツ領域を統合できる。

正確な座標と領域比率は、試作後に確定します。

## 4. レイアウトの選択順

次の順序で選択します。

1. 表紙、アジェンダ、出典などの特殊ページかを確認する。
2. `10_CONTENT.md`で設定した主要ジョブを確認する。
3. 文章、画像、図解、グラフ、表、手順などの主要素材を確認する。
4. 情報量がファミリーの収容量に収まるか確認する。
5. `standalone`、`presented`、`training`に合わせて面積比率と密度を調整する。
6. 前後のスライドとの比較可能性と見え方を確認する。

内容への適合性を、レイアウトの多様性より優先します。

## 5. スライドジョブとの対応

| スライドジョブ | 第一候補 | 内容に応じた候補 |
|---|---|---|
| `claim` | `single_message` | `data_focus`、`summary_action` |
| `explain` | `text_focus` | `text_visual`、`process` |
| `evidence` | `data_focus` | `text_visual`、`comparison` |
| `compare` | `comparison` | `data_focus` |
| `instruction` | `process` | `text_focus`、`exercise` |
| `question` | `single_message` | `exercise` |
| `exercise` | `exercise` | 原則固定 |
| `summary` | `summary_action` | `single_message` |
| `transition` | `section_divider` | 原則固定 |

特殊ページは主要ジョブよりページの用途を優先します。

- 表紙は`cover`を使用する。
- アジェンダは`agenda`を使用する。
- 出典、用語集、補足表は`sources_appendix`を使用する。

## 6. 初版レイアウト一覧

初版は12ファミリー、16レイアウトとします。

| ファミリー | バリエーション数 | 初版レイアウト |
|---|---:|---|
| `cover` | 1 | 標準表紙 |
| `agenda` | 1 | セクション一覧 |
| `section_divider` | 1 | 章扉 |
| `single_message` | 1 | 単一メッセージ |
| `text_focus` | 1 | 文章中心 |
| `text_visual` | 2 | 文字左・画像右、画像左・文字右 |
| `comparison` | 2 | 2項目比較、3項目比較 |
| `process` | 2 | 横方向、縦方向 |
| `data_focus` | 2 | グラフ中心、表中心 |
| `exercise` | 1 | 演習・問い |
| `summary_action` | 1 | まとめ・次の行動 |
| `sources_appendix` | 1 | 出典・補足 |

## 7. ファミリー別仕様

### 7.1 `cover`

用途:

- 資料の表紙

必須領域:

- タイトル
- 日付
- ページ番号

任意領域:

- サブタイトル
- 作成者・組織名
- テーマに沿った背景装飾

選択条件:

- 資料の先頭で使用する。

使用しない条件:

- 章の切り替えには使用せず、`section_divider`を使用する。

収容量:

- タイトル、日付、任意のサブタイトルと作成者・組織名に限定する。
- 表紙へ概要、アジェンダ、長い説明を追加しない。

### 7.2 `agenda`

用途:

- 資料全体の構成または研修の進行を示す。

必須領域:

- タイトル
- セクション一覧
- ページ番号

選択条件:

- `10_CONTENT.md`のアジェンダ使用条件を満たす場合に使用する。

使用しない条件:

- 短い資料へ慣習だけで追加しない。
- アジェンダを資料の物語構成の代わりにしない。

収容量:

- 3〜5セクションを目安とする。
- 各セクションは短い名称または到達点で示す。

### 7.3 `section_divider`

用途:

- 章、論点、学習段階を切り替える。

必須領域:

- 章タイトル
- ページ番号

任意領域:

- 短い説明または次の問い
- 大きな背景装飾

選択条件:

- 論理上の区切りがあり、読み手に切り替えを認識させる必要がある。

使用しない条件:

- 1〜2枚ごとに機械的に挿入しない。

収容量:

- 章タイトルと任意の短い説明に限定する。

### 7.4 `single_message`

用途:

- 重要な主張、問い、数値、結論を1つ強く示す。

必須領域:

- 主要メッセージ
- ページ番号

任意領域:

- 短い補足
- 数値の単位、期間、比較対象
- 短い出典

選択条件:

- 1つの文または数値だけでスライドの主要ジョブを達成できる。

使用しない条件:

- 複数の根拠や長い説明が必要な場合は使用しない。

収容量:

- 1つの主張、問い、重要数値に限定する。

### 7.5 `text_focus`

用途:

- 文章中心の説明、定義、注意点、短い手順を示す。

必須領域:

- タイトル
- 本文
- ページ番号

任意領域:

- 小見出し
- 短い出典
- 小さな補助装飾

選択条件:

- 画像や図表がなくても、文章だけで最も明確に説明できる。

使用しない条件:

- 視覚情報が理解を大きく改善する場合は`text_visual`または`data_focus`を検討する。

収容量:

- 2〜4項目、または1〜2段落を目安とする。
- 文字量は`10_CONTENT.md`のモード別基準に従う。

### 7.6 `text_visual`

用途:

- 文章と写真、イラスト、図解を組み合わせる。

必須領域:

- タイトル
- 本文
- 主要ビジュアル1点
- ページ番号

任意領域:

- 短い出典
- ビジュアルの短い説明

バリエーション:

- 文字左・ビジュアル右
- ビジュアル左・文字右

選択条件:

- ビジュアルが現在の主張または説明を実質的に補強する。
- ビジュアルの配置方向が、被写体の向き、余白、読み順に合う。

使用しない条件:

- 装飾のためだけに画像領域を作らない。
- 適切なビジュアルがない場合は`text_focus`を使用する。

収容量:

- 本文2〜3項目と主要ビジュアル1点を目安とする。
- 文章とビジュアルの初期比率は40:60から60:40の範囲で試作する。

### 7.7 `comparison`

用途:

- 選択肢、変更前後、対象間の違いを同じ軸で比較する。

必須領域:

- タイトル
- 比較対象の見出し
- 共通の比較軸
- ページ番号

任意領域:

- 推奨または結論
- 短い出典

バリエーション:

- 2項目比較
- 3項目比較

選択条件:

- 2〜3対象を同じ観点で並べることで違いが明確になる。

使用しない条件:

- 比較軸が対象ごとに異なる場合は使用しない。
- 4対象以上を無理に横並びにしない。

収容量:

- 2〜3対象、各1〜3項目を目安とする。
- 3項目比較は、各対象の文章を2項目程度までに抑える。

### 7.8 `process`

用途:

- 手順、時系列、進行、因果の流れを示す。

必須領域:

- タイトル
- 順序が分かるステップ
- ページ番号

任意領域:

- 開始条件
- 完了条件
- 注意点

バリエーション:

- 横方向の流れ
- 縦方向の手順

選択条件:

- 順序またはつながりが理解に不可欠である。

使用しない条件:

- 独立した箇条書きへ、意味のない矢印や順番を付けない。

収容量:

- 横型は3〜5ステップ、縦型は3〜6ステップを目安とする。
- 1ステップには1つの行動または状態を書く。
- 導入文は120文字以内を目安とし、主要条件だけを残す。
- ステップの見出しと説明で同じ文を繰り返さない。
- 導入文が上限を超える場合は、詳細を対応するステップへ移すか、スライドを分割する。

### 7.9 `data_focus`

用途:

- グラフ、表、主要数値と、その読み取り結果を示す。

必須領域:

- 結論を示すタイトル
- 主要なグラフまたは表1点
- 読み取り結果
- 出典
- ページ番号

バリエーション:

- グラフ中心
- 表中心

選択条件:

- データが現在の主張または判断の主要な根拠である。

使用しない条件:

- データを並べるだけで意味を説明できない場合は使用しない。
- 複数の無関係なグラフを1枚へ詰め込まない。

収容量:

- 主要なグラフまたは表1点と、1つの読み取り結果に限定する。
- 主要数値の前に置く導入文は80文字以内を目安とする。
- 大きく表示する数値またはキーワードは18文字以内とし、対象名、単位、条件、解釈を同じ文字サイズで連結しない。
- 対象名、主要数値、補足、読み取り結果、実行条件を別の表示階層へ分ける。
- 読み取り結果と実行条件は各80文字以内を目安とし、同じ結論を複数領域で繰り返さない。
- 表の行列数は試作後に確定し、詳細表は`sources_appendix`へ分ける。

反復する項目の概要と詳細:

- 曜日別メニューなど、同じ項目に「役割」「実施方法」「変更条件」がある場合は、短ければ1枚の表にまとめる。
- 1行が90文字を超える、または3項目のセル本文合計が180文字を超える場合は、概要1枚と項目別の詳細へ分割する。
- 概要には役割と強度だけを残し、詳細には実施方法、目的、変更条件を置く。
- 詳細ページ数を固定せず、入力された項目数と説明量から決定する。

### 7.10 `exercise`

用途:

- 問い、演習、理解確認、参加者への指示を示す。

必須領域:

- 実行してほしい行動または問い
- 手順
- 所要時間
- 完了条件
- ページ番号

任意領域:

- 演習の目的
- ヒント
- 回答方法

選択条件:

- 読み手または参加者に考える、書く、話す、操作するなどの行動を求める。

使用しない条件:

- 通常の説明を演習の形に見せない。

収容量:

- 手順は1〜3ステップを目安とする。
- 問いと答え・解説は原則として別スライドにする。

### 7.11 `summary_action`

用途:

- 要点をまとめ、次の行動、判断、練習へつなげる。

必須領域:

- まとめまたは結論
- ページ番号

任意領域:

- 最大3つの要点
- 次の行動または求める判断
- 参照先

選択条件:

- 資料または章の終わりで、意味と次の行動を明確にする必要がある。

使用しない条件:

- 本編の内容をそのまま繰り返すだけの場合は使用しない。
- 「ありがとうございました」だけの終了ページにしない。

収容量:

- 最大3つの要点と、1つの次の行動を目安とする。

### 7.12 `sources_appendix`

用途:

- 完全な出典一覧、用語集、補足表、バックアップ情報を示す。

必須領域:

- ページの用途が分かるタイトル
- 補足内容
- ページ番号

選択条件:

- 本編の理解を妨げず、参照価値のある詳細を残す必要がある。

使用しない条件:

- 判断に重要なリスクや、本編理解に不可欠な情報を付録だけに置かない。

収容量:

- 可読性を保てる件数に分割する。
- 出典、用語集、補足表を同じページへ無理に混在させない。
- 1ページの正確な件数と表の行列数は試作後に確定する。

## 8. コンテンツモードによる調整

| モードまたは用途 | レイアウト調整 |
|---|---|
| `presented` | 主要領域を大きくし、要素数を減らし、視覚的な焦点を1つにする |
| `standalone` | 補足、注釈、出典を収める領域を確保し、情報群の境界を明確にする |
| `training` | 演習、手順、時間、完了条件、理解確認を見つけやすくする |

- `MUST`: モードだけを理由に別のレイアウトファミリーへ切り替えない。
- `MUST`: `standalone`の情報量を、文字サイズの縮小やカードの増加だけで処理しない。
- `SHOULD`: `presented`では`single_message`、`text_visual`、`data_focus`の主要領域を広く取る。
- `SHOULD`: `training`では、同じ演習形式を繰り返す場合に配置を統一し、参加者が毎回読み方を学び直さないようにする。

## 9. 収容量を超えた場合

レイアウトの収容量を超えた場合は、`10_CONTENT.md`の文字量と情報密度の調整手順へ戻ります。

- `MUST`: 最小文字サイズを下回らない。
- `MUST`: 4カラム以上へ増やして無理に収めない。
- `MUST`: 主要ビジュアルを複数詰め込み、焦点を失わせない。
- `SHOULD`: 重複の削除、表現の短縮、補足の移動、スライド分割、別レイアウトの順で再検討する。
- `SHOULD`: 空きが大きい場合は、装飾で埋めず、より単純なレイアウトを選ぶ。

どの型にも適合しない場合は、新しいレイアウトを即興で追加する前に、スライドジョブまたは資料構成の分割を検討します。

## 10. 連続使用と資料全体のリズム

同じレイアウトの連続使用を禁止しません。各ページに最適なレイアウトであることを優先します。

- `MUST`: 見た目を変えることだけを目的に、不適切なレイアウトへ変更しない。
- `SHOULD`: 比較、複数事例、連続手順など、同じ構造が理解に役立つ場合は配置を統一する。
- `SHOULD`: 同じレイアウトが続く場合も、主要メッセージと視覚的な焦点を各ページで明確にする。
- `SHOULD`: 連続使用が内容上の理由ではなく、選択の惰性になっていないか確認する。
- `MAY`: 比較可能性を損なわない範囲で、左右配置または面積比率を調整できる。

連続使用は違反ではなく、レビュー時の確認対象とします。

## 11. 避ける配置

- 内容を小さなカードへ分割しすぎる。
- 見出し、本文、図表、注釈を同じ強さで並べる。
- 画像がないのに空の画像領域を残す。
- 独立した情報へ意味のない矢印を付ける。
- 2項目の比較を、異なる大きさや異なる軸で見せる。
- グラフや表を配置するだけで、読み取り結果を示さない。
- 余白を装飾図形で埋める。
- タイトルまたはフッター領域へ本文を侵入させる。
- レイアウトを変えるためだけに、内容の順序や論理を変える。

## 12. 試作と検証

正確な座標、面積比率、収容量は、次の手順で検証した後に確定します。

1. 初版16レイアウトを代表的なサンプル内容で一覧化する。
2. 表紙、文章中心、図表中心、比較、手順、演習、出典を重点確認する。
3. `standalone`、`presented`、研修・教材の短いデッキを作成する。
4. HTML/PDFとPPTX/PDFで表示差を確認する。
5. 各レイアウトを「採用」「要調整」「不採用」で判定する。
6. 採用したレイアウトだけ、座標、面積比率、収容量を確定する。

### 12.1 検査項目

- 主要ジョブに適しているか
- タイトルと主要メッセージが最初に認識できるか
- 本文、画像、図表、出典、ページ番号が重なっていないか
- 余白が窮屈または過剰になっていないか
- 最小文字サイズを維持できるか
- HTMLとPPTXで情報階層が一致するか
- PDF変換後も欠け、折り返し、位置ずれがないか
- 同じレイアウトが続いても読み進めやすいか
- `standalone`、`presented`、`training`で使い分けられるか

### 12.2 試作後に確定する値

- 左右、上下の正確な安全余白
- タイトル、本文、ビジュアル、図表の座標と面積比率
- ファミリーごとの最大文字量
- 比較カラムの幅と最大項目数
- 表の最大行列数
- 画像の推奨アスペクト比
- 出典一覧の1ページあたりの件数
- HTMLとPPTXで共通化できる値と形式別に分ける値

試作結果に基づいて変更した場合は、`20_DESIGN.md`の暫定余白と文字サイズも合わせて見直します。
