---
spec_name: slide-system-design
version: 0.2.0
status: draft
updated: 2026-08-04
language: ja
default_theme: warm_clean
---

# Design Specification

## 1. 目的

この仕様は、スライドのテーマ、トンマナ、色、文字、余白、整列、装飾、強調、固定要素、可読性を定義します。

内容の主役を明確にしながら、ビジネス資料と研修・教材の両方に使える、温かく端正なデザインを一貫して生成することを目的とします。

具体的なレイアウトタイプと配置条件は`30_LAYOUTS.md`、写真、イラスト、図解、グラフの選択と表現は`40_VISUALS.md`、形式別の実装と検査は`50_OUTPUTS.md`に従います。

## 2. テーマ構造

デザインは、すべてのテーマに適用する共通原則と、テーマ固有の設定に分けます。

```text
共通原則
├─ 可読性
├─ 情報の階層
├─ コントラスト
├─ 余白と整列
└─ 一貫性

テーマ固有設定
├─ カラーパレット
├─ フォント
├─ 図形と装飾
├─ 背景
└─ 強調表現
```

- v1の既定テーマは`warm_clean`とする。
- ユーザーがテーマを指定しない場合は`warm_clean`を使用する。
- 資料タイプやコンテンツモードだけを理由に、別テーマへ自動的に切り替えない。
- 将来、`business`や`education`などのテーマを追加できる構造を維持する。
- 色、フォント、装飾の値を変更しても、共通の可読性ルールは維持する。

### 2.1 テーマ拡張契約

新しいテーマを追加する場合は、少なくとも次を定義します。

- テーマID、表示名、バージョン
- 目指す印象と避ける印象
- カラーパレットと各色の役割
- フォントと代替フォント
- 背景、情報面、濃色背景の使い分け
- 図形、角丸、線、影、装飾の方針
- 余白、整列、情報階層、強調の方針
- 写真、イラスト、アイコン、図解、グラフ、表の表現
- アクセシビリティとコントラスト要件
- 代表ページと出力形式による検証結果

- `MUST`: テーマは本仕様の共通可読性ルールを上書きしない。
- `MUST`: テーマは`10_CONTENT.md`の内容ルールと`30_LAYOUTS.md`の収容量を上書きしない。
- `MUST`: 資料タイプだけを理由にテーマを自動選択しない。
- `MUST`: 表紙、文章中心、データ中心の代表ページを、HTML/PDFとPPTX/PDFで検証する。
- `MUST`: 指定テーマが存在しない、必要項目が不足する、または利用できない場合は`warm_clean`へ戻し、その旨を伝える。

第2テーマを追加するまではテーマ別ファイルへ分割しません。追加時には、共通契約を`themes/THEME_TEMPLATE.md`、既定テーマを`themes/warm_clean.md`、追加テーマを`themes/<theme_id>.md`へ分離できます。

## 3. テーマとブランド指定の優先順位

デザインに関する指定は、次の順序で扱います。

1. ユーザーが提供したPowerPointテンプレートまたは既存スライド
2. ユーザーが提供したブランドガイド
3. 明示された色、フォント、ロゴ、参考デザイン
4. `warm_clean`の既定値

- `MUST`: テンプレートがある場合は、その構造とデザインを優先し、`warm_clean`を無理に混ぜない。
- `MUST`: ロゴは提供されたものだけを使用し、生成または推測しない。
- `MUST`: ブランド指定があっても、共通の最小文字サイズ、コントラスト、色だけに依存しないルールを維持する。
- `SHOULD`: ブランドカラーだけが指定された場合は、`warm_clean`のアクセント色を置き換え、背景、文字階層、余白の考え方は維持する。
- `SHOULD`: ブランドカラーのコントラストが不足する場合は、本文文字ではなく装飾、線、背景面に限定する。
- `SHOULD`: 指定フォントが利用できない場合は、利用可能な代替フォントと影響を明示する。

## 4. `warm_clean`のトンマナ

`warm_clean`は、次の印象を目指します。

- 明快
- 落ち着いている
- 信頼感がある
- 現代的だが流行に寄りすぎない
- 無機質になりすぎず、わずかに柔らかい
- ビジネス資料にも研修・教材にも使える

次の表現は避けます。

- 過度に派手、かわいい、高級、感情的な表現
- 装飾を主役にした構成
- UIのようなカード、タブ、ボタン、バッジの多用
- 同じ見た目のスライドの機械的な連続
- グラデーション、立体表現、光沢表現の多用

## 5. カラーパレット

### 5.1 既定カラー

| トークン | カラー | 主な用途 |
|---|---|---|
| `background` | `#FFF8F4` | 基本背景 |
| `surface` | `#FFFFFF` | 情報を区切る面、図表の面 |
| `text_primary` | `#3F3639` | タイトル、本文、主要な数値 |
| `text_secondary` | `#665B5E` | 補足、注釈、二次情報 |
| `coral_text` | `#B94D48` | 小見出し、重要語、強調文字 |
| `coral_accent` | `#E96E68` | 線、アイコン、小さな図形 |
| `coral_soft` | `#FFD2C7` | 大きな背景図形、領域分け |
| `mint_soft` | `#C5E7E1` | 補足、比較対象の背景図形 |
| `divider` | `#E8DFDC` | 区切り線、罫線 |
| `dark_background` | `#3F3639` | 強い区切り、重要な結論の背景 |
| `inverse_text` | `#FFF8F4` | 濃色背景上の文字 |

`background`の`#FFF8F4`を`warm_clean`の基準色とします。アクセント色の正確な値は、試作結果を見て調整できる暫定値です。1つの資料内では、採用した値を統一します。

### 5.2 色の使用ルール

- `MUST`: 本文と主要タイトルには`text_primary`を使用する。
- `MUST`: `coral_soft`と`mint_soft`を本文文字色に使用しない。
- `MUST`: 色だけで情報の違いや状態を伝えない。
- `SHOULD`: コーラルを主要な強調、ミントを補足、別視点、比較対象に使用する。
- `SHOULD`: 通常の1枚で主要なアクセント色を増やしすぎない。
- `SHOULD`: 成功、注意、危険などの色は、状態を伝える必要がある場合だけ追加する。
- `SHOULD`: データ系列が多い場合は、`warm_clean`の装飾色だけで対応せず、`40_VISUALS.md`の図表ルールに従う。

## 6. タイポグラフィ

### 6.1 フォント

既定の日本語フォントは`Noto Sans JP`です。利用できない場合は、次の順序で代替します。

```text
Noto Sans JP
Hiragino Sans
Yu Gothic / YuGothic
Meiryo
sans-serif
```

- `MUST`: 1つの資料内では原則として1つの書体ファミリーを使用する。
- `MUST`: フォントを変更しても、タイトル、見出し、本文の階層を維持する。
- `SHOULD`: タイトルはBold、小見出しはMediumまたはBold、本文はRegularを使用する。
- `SHOULD`: 強調だけを目的に別書体を追加しない。
- `SHOULD`: 長い文章全体を太字にせず、重要語または重要な数値だけを強調する。

### 6.2 基準サイズ

次の値を16:9スライドの基準とします。

| 要素 | 基準サイズ |
|---|---:|
| 表紙タイトル | 50〜56pt |
| 章タイトル | 42〜48pt |
| スライドタイトル | 35〜40pt |
| 小見出し、コールアウト見出し | 24〜28pt |
| `presented`の本文 | 22〜26pt |
| `standalone`の本文 | 18〜22pt |
| 注釈、出典、ページ番号 | 10〜14pt |

- `MUST`: 表紙タイトルは50pt、スライドタイトルは35pt、小見出しは24pt、本文は16ptを下回らない。
- `MUST`: 1行を想定したタイトルや見出しを、意図せず2行に折り返さない。
- `MUST`: 文字が収まらない場合は、文字を縮小する前に文章の短縮、レイアウト変更、スライド分割を検討する。
- `SHOULD`: `standalone`でも、表示領域と内容量が許す限り20pt以上の本文を優先する。
- `SHOULD`: 本文の行間は読みやすさを保ち、行同士が接近しすぎないようにする。

## 7. 図形と装飾

`warm_clean`では、柔らかい図形と端正な文字組みを組み合わせます。

- `MUST`: 装飾をタイトル、本文、重要な図表より目立たせない。
- `MUST`: 装飾図形が文字や重要な図表へ意図せず重ならないようにする。
- `MUST`: 意味のない装飾を、情報を表す図形と誤認させない。
- `SHOULD`: 円、楕円、緩やかな曲線を主要な装飾モチーフとする。
- `SHOULD`: 大きな装飾は1枚に1〜2個程度を目安とする。
- `SHOULD`: 大きな円や曲線は表紙、章扉、単一メッセージのスライドを中心に使用する。
- `SHOULD`: 本編では、短い線、小さな円、淡い背景面として限定的に使用する。
- `SHOULD`: 影、グラデーション、立体表現、光沢表現は原則として使用しない。
- `SHOULD`: 角丸は情報群を分ける必要がある場合に使用し、すべての要素をカード化しない。
- `SHOULD`: 同じ円形モチーフを全ページへ機械的に配置しない。
- `SHOULD`: スライド全体を角丸の外枠で囲むデザインを共通要素にしない。

背景装飾はスライド端からはみ出すことができます。文字、ロゴ、出典、ページ番号などの意味を持つ要素は、安全余白内に置きます。

## 8. 余白と整列

### 8.1 暫定既定値

次の値は試作前の`SHOULD`であり、レイアウト検証後に調整できます。

- 左右の安全余白はスライド幅の5〜6％とする。
- 上下の安全余白はスライド高の5％程度とする。
- タイトルと本文の間に、情報階層が分かる余白を取る。
- 関連する要素は近づけ、異なる情報群は広く離す。

### 8.2 整列ルール

- `MUST`: 重要な文字、図表、出典、ページ番号を安全余白の外へ置かない。
- `MUST`: 同じ役割の要素は、スライド間で共通の整列線に合わせる。
- `MUST`: 余白を確保するために、最小文字サイズを下回らない。
- `SHOULD`: 左揃えを基本とする。
- `SHOULD`: 中央揃えは、表紙、章扉、単一メッセージなど、中央配置に意味がある場合だけ使用する。
- `SHOULD`: 左右の安全余白は均等にし、内部の構成は非対称にできる。
- `SHOULD`: 本文の均等割付を使用しない。

具体的なカラム、画像比率、図表領域、座標は`30_LAYOUTS.md`で定義します。

## 9. コンテンツモードによる調整

色、フォント、図形の基本は共通にし、情報密度と強調方法を調整します。

| モードまたは用途 | デザイン方針 |
|---|---|
| `presented` | 文字を大きくし、要素を減らし、1つの視覚的な焦点を作る |
| `standalone` | 説明、注釈、出典を収めながら、情報群を明確に分ける |
| `training` | 学習目標、演習、注意点、理解確認を識別しやすくする |

- `MUST`: モードによる調整だけを理由に、別のテーマへ切り替えない。
- `MUST`: `standalone`でも、読みにくい小さな文字や過密なカード構成にしない。
- `SHOULD`: `training`でも、色や装飾を増やしすぎず、`warm_clean`の範囲で演習と問いを識別させる。

## 10. 強調と情報階層

強調は次の3段階に限定します。

1. 主役: タイトル、結論、重要数値
2. 補助: 小見出し、比較対象、注目語
3. 通常: 本文、説明、注釈

- `MUST`: 1枚の中で最も強い強調は原則1箇所にする。
- `MUST`: 色だけで強調レベルを表さない。
- `SHOULD`: まず文字サイズと太さ、次に色、最後に背景図形または囲みを使用する。
- `SHOULD`: コーラルを主要な結論、重要語、現在地の強調に使用する。
- `SHOULD`: ミントを補足、別視点、比較対象の背景に使用する。
- `SHOULD`: 太字、色、囲みを同じ箇所へ重ねすぎない。
- `SHOULD`: 下線はリンク以外では原則使用しない。

## 11. 背景の使い分け

- 通常スライドは`background`を使用する。
- 情報を区切る面には`surface`を使用できる。
- 表紙と章扉は、`background`上に大きなコーラルまたはミントの図形を使用できる。
- 全面コーラルまたは全面ミントの背景は使用しない。
- `dark_background`は、強い区切りまたは重要な結論でのみ使用できる。
- 濃色背景は、1資料の10〜15％程度までを暫定`SHOULD`とする。
- 白い面を多数並べたカード型レイアウトは避ける。
- 背景の変更には、章の切り替えや情報上の意味を持たせる。

## 12. 固定要素

### 12.1 ページ番号

- `MUST`: 表紙を`01`として、表紙、章扉、本編、付録、出典を含む全ページに番号を表示する。
- `MUST`: `01`、`02`のような2桁表記を使用する。
- `MUST`: ページ番号を右下の共通位置へ置く。
- `SHOULD`: ページ番号は`text_primary`で小さく表示し、本文より目立たせない。

HTMLの操作用ページ表示と、スライド内のページ番号は別の要素として扱います。

### 12.2 出典とその他の固定要素

- `MUST`: スライド内に表示する短い出典は左下の共通位置へ置く。
- `MUST`: 出典とページ番号が重ならない領域を確保する。
- `MUST`: 組織名やロゴは、ユーザーから提供された場合だけ表示する。
- `SHOULD`: 資料タイトルを全ページのフッターへ繰り返さない。

出典の記載内容は`10_CONTENT.md`に従います。

## 13. 可読性とアクセシビリティ

### 13.1 すべてのテーマに共通する`MUST`

- 最小文字サイズを下回らない。
- 通常文字と背景は4.5:1以上、大きな文字は3:1以上のコントラスト比を確保する。
- 色だけで意味、状態、分類を区別しない。
- 色分けには、ラベル、線種、記号、配置のいずれかを併用する。
- 重要な文字を、写真や装飾図形の上へ読みにくい状態で直接配置しない。
- グラフの系列を色だけに依存させない。
- PDF変換後も、文字と背景の識別性を確認する。
- 意図しない重なり、欠け、折り返しを修正する。

### 13.2 `warm_clean`固有の`MUST`

- `coral_soft`と`mint_soft`を本文文字色に使用しない。
- 淡色背景の上には`text_primary`または十分なコントラストを持つ文字色を使用する。
- コーラルとミントの違いだけで分類しない。
- プロジェクター表示を想定し、淡すぎる線や小さな文字を重要情報に使用しない。

`standalone`では、必要に応じてグレースケールでも論旨と主要な分類が理解できるか確認します。

## 14. 試作後に見直す項目

次の値は、表紙、文章中心の本編、図表中心の本編を試作し、HTML、PPTX、PDFで確認してから確定します。

- アクセントカラーの正確な値
- 左右余白5〜6％と上下余白5％
- 大きな装飾を1〜2個とする目安
- 円、曲線、角丸の使用量
- `standalone`と`presented`の本文サイズ
- 濃色背景を10〜15％までとする目安

見直しでは、好みだけでなく、可読性、情報階層、変換後の再現性、資料タイプへの適合性を確認します。
